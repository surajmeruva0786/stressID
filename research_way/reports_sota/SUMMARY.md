# SOTA campaign — summary tables

Generated by `python -m src.sota_summary`.

> **Protocol.** Subject-shared repeated stratified CV, the split
> rule the StressID origin paper uses. Subjects appear on both
> sides; scores are inflated by subject leakage by a margin this
> repo measured separately (see `LEAKY_PROTOCOL.md`). These numbers
> are comparable to published ones and **not** to the
> subject-held-out track in `reports/`.

## 1. Round progression (seed 42 — the partition the search used)

| Run | Macro F1 | Weighted F1 | Accuracy | ROC AUC |
|---|---|---|---|---|
| `s1_baseline_raw` | 0.7419 | 0.7426 | 0.7429 | 0.8091 |
| `s2_views_relz` | 0.7517 | 0.7525 | 0.7529 | 0.8133 |
| `s3_prune` | 0.7505 | 0.7512 | 0.7514 | 0.8154 |
| `s4_windows` | 0.7572 | 0.7580 | 0.7586 | 0.8228 |
| `s5_final` | 0.7604 | 0.7612 | 0.7614 | 0.8226 |

These are **not** the numbers to publish: all five rounds
selected against this one partition, so the maximum over it is
optimistically biased. Section 3 quantifies by how much.

## 2. Paired comparisons (identical folds)

| Comparison | Scope | Folds | A | B | Δ | 95% CI | Wins | t p | Wilcoxon p |
|---|---|---|---|---|---|---|---|---|---|
| `s6_confirm_seed101` vs `s7_baseline_seed101` | all700 | 5 | 0.7467 | 0.7272 | **+0.0195** | [-0.0178, +0.0569] | 3/5 | 0.2204 | 0.3125 |
| `s6_confirm_seed101` vs `s7_baseline_seed101` | c364 | 5 | 0.6721 | 0.6605 | **+0.0117** | [-0.0314, +0.0547] | 3/5 | 0.4937 | 0.4375 |

A confidence interval spanning zero means the round-by-round
gains are not individually established at this fold count. That is
reported rather than hidden: the defensible claim is about the
pipeline as a whole, not each increment.

## 3. Campaign selection bias

| Configuration | Partition | Macro F1 |
|---|---|---|
| final | seed 42 — searched | 0.7604 |
| final | seed 101 — unseen | 0.7467 |

**Measured selection bias: +0.0137 macro F1.** Five rounds of choosing against one partition bought this much apparent score that does not transfer.

## 4. Against published work

| Source | Protocol | Weighted F1 | Macro F1 |
|---|---|---|---|
| StressID paper (SVM + average-rule fusion) | random 80/20 + SMOTE | **0.72** | not reported |
| This repo, prior `decision_fusion+rf` | random 80/20 | 0.726 | 0.728 |
| **This campaign** (searched partition) | subject-shared 5-fold | 0.761 | 0.760 |
| **This campaign** (unseen partition) | subject-shared 5-fold | 0.748 | 0.747 |
| Majority class | — | — | 0.344 |

The paper's 0.72 is a weighted F1 under a random 80/20 split with
SMOTE; ours is a weighted F1 under subject-shared 5-fold CV without
SMOTE. Same family of protocol, not an identical one, and the
comparison should be described that way rather than as a
like-for-like win.
